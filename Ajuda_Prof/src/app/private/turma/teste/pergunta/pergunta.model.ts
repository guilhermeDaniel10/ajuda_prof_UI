export class Pergunta {
    iteracao: number;
    pergunta: number;
    cotacao: number;

    constructor(iteracao: number, pergunta: number, cotacao: number) {
        this.iteracao = iteracao;
        this.pergunta = pergunta;
        this.cotacao = cotacao;
    }
}
